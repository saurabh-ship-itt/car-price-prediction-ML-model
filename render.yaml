services:
  - type: web
    name: car-price-prediction
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run car-app.py --server.address=0.0.0.0 --server.port=$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10